<<<<<<< HEAD
﻿@{
    Layout = "~/Views/Shared/_Layout.cshtml"; // Set the correct layout file if you have one
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Attendance Requests</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        /* Include your CSS styling here */
    </style>
</head>
<body>
    <div class="content">
        <h2>Pending Attendance Requests</h2>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Request ID</th>
                    <th>Employee Name</th>
                    <th>Date</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @* Hardcoded sample data *@
                <tr>
                    <td>1</td>
                    <td>Jane Doe</td>
                    <td>2024-08-20</td>
                    <td>Pending</td>
                    <td>
                        <a href="@Url.Action("ApproveAttendance", "Manager", new { id = 1 })" class="btn btn-success btn-sm">Approve</a>
                        <a href="@Url.Action("RejectAttendance", "Manager", new { id = 1 })" class="btn btn-danger btn-sm">Reject</a>
                    </td>
                </tr>
                <tr>
                    <td>2</td>
                    <td>John Smith</td>
                    <td>2024-08-21</td>
                    <td>Pending</td>
                    <td>
                        <a href="@Url.Action("ApproveAttendance", "Manager", new { id = 2 })" class="btn btn-success btn-sm">Approve</a>
                        <a href="@Url.Action("RejectAttendance", "Manager", new { id = 2 })" class="btn btn-danger btn-sm">Reject</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>




@*@foreach (var request in ViewBag.AttendanceRequests)
    {
        <tr>
            <td>@request.EmployeeName</td>
            <td>@request.Date</td>
            <td>@request.Status</td>
        </tr>
    }*@

=======
﻿@model IEnumerable<ALMSystem2.Models.Attendance>
@{
    ViewBag.Title = "Attendance Requests";
}

<h2>Attendance Requests</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Attendance ID</th>
            <th>Employee ID</th>
            <th>Project ID</th>
            <th>Attendance Date</th>
            <th>Status</th>
            <th>ApprovalStatus</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.AttendanceID)</td>
                <td>@Html.DisplayFor(modelItem => item.EmployeeID)</td>
                <td>@Html.DisplayFor(modelItem => item.ProjectID)</td>
                <td>@Html.DisplayFor(modelItem => item.AttendanceDate)</td>
                <td>@Html.DisplayFor(modelItem => item.Status)</td>
                <td>@Html.DisplayFor(modelItem => item.ApprovalStatus)</td>
                <td>
                    @using (Html.BeginForm("ApproveAttendanceRequest", "Manager", FormMethod.Post, new { id = item.AttendanceID }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("id", item.AttendanceID)
                        <input type="hidden" name="id" value="" />
                        <button type="submit" class="btn btn-success">Approve</button>
                    }
                    @using (Html.BeginForm("RejectAttendanceRequest", "Manager", FormMethod.Post, new { id = item.AttendanceID }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("id", item.AttendanceID)
                        <input type="hidden" name="id" value="" />
                        <button type="submit" class="btn btn-danger">Reject</button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="@Url.Action("ManagerDashboard", "Manager")" class="btn btn-primary">Back to Dashboard</a>
>>>>>>> 7f696cdbb8726d085feec7d422b8c0b4898de8d0

